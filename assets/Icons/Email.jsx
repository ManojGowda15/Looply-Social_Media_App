import React from "react";
import { Entypo } from "@expo/vector-icons";
import { theme } from "../../constants/theme";

const Email = () => {
  return <Entypo name="email" size={20} color={theme.colors.textLight} />;
};

export default Email;
