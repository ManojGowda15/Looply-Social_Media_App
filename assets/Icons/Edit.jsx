import React from "react";
import { theme } from "../../constants/theme";
import { Feather } from "@expo/vector-icons";

const Edit = () => {
  return <Feather name="edit-3" size={24} color={theme.colors.textLight} />;
};

export default Edit;
