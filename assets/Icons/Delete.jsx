import React from "react";
import { theme } from "../../constants/theme";
import { MaterialIcons } from "@expo/vector-icons";

const Delete = () => {
  return <MaterialIcons name="delete-outline" size={24} color="white" />;
};

export default Delete;
