import React from "react";
import { Ionicons } from "@expo/vector-icons";
import { theme } from "../../constants/theme";

const Call = () => {
  return <Ionicons name="call-outline" size={24} color={theme.colors.text} />;
};

export default Call;
